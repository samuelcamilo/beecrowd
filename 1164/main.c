#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    int cont = 0;
    int valor;

    scanf("%d", &n);
    while(cont < n){

        scanf("%d", &valor);

        cont++;
    }

    return 0;
}
